def recognize_face(image_bytes: bytes) -> dict:
    """Placeholder untuk FaceNet recognition."""
    return {
        "matched": False,
        "user_id": None,
        "distance": None,
        "message": "FaceNet recognition belum diimplementasikan.",
        "image_size_bytes": len(image_bytes),
    }

